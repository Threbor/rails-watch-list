<div class="banner-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/breakfast.jpg)">
Lists(Index)
</div>
<div class="d-flex justify-content-between m-5">
<h2>My list</h2>
<%= link_to 'new list',  new_list_path, class: "btn btn-flat" %></a>
</div>

<div>
  <div class="cards m-5">
  <%@lists.each do |list| %>
    <div class="card"
    style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= if list.photo.attached? then cl_image_path list.photo.key, height: 300, crop: :fill end %>')"
    >
    <li><%= link_to list.name, list_path(list) %></li>
    </div>
  <% end %>
  </div>
</div>
